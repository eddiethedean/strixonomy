name: CI

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: 1

permissions:
  contents: read

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - run: cargo fmt --all -- --check

  doc-versions:
    name: documentation version sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - run: chmod +x scripts/check-doc-versions.sh
      - run: ./scripts/check-doc-versions.sh
      - name: Install ripgrep
        run: sudo apt-get update && sudo apt-get install -y ripgrep
      - run: chmod +x scripts/check-strixonomy-rename.sh
      - run: ./scripts/check-strixonomy-rename.sh

      - name: Install PyYAML
        run: pip install --user pyyaml

      - name: Validate parity manifest (schema + paths)
        run: python3 scripts/validate-parity-manifest.py --paths

      - name: Validate Protégé test-port inventory
        run: python3 scripts/validate-protege-test-port.py

      - name: Parity release-gate report (infra)
        run: python3 scripts/check-parity-release-gate.py

      - name: Parity docs sync check
        run: python3 scripts/generate-parity-docs.py --check

  python-package:
    name: python package (reservation)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build reservation wheel
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip build
          python -m build python/

      - name: Smoke import
        run: |
          set -euo pipefail
          pip install python/dist/strixonomy-*.whl
          python -c "import strixonomy; assert strixonomy.status()['reservation']"

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - uses: Swatinem/rust-cache@v2

      - run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Free disk space
        run: |
          set -euo pipefail
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force || true

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - run: cargo build -p strixonomy-lsp -p strixonomy-cli --bins

      - run: cargo test --workspace

  msrv:
    name: MSRV (1.88)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Free disk space
        run: |
          set -euo pipefail
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force || true

      - uses: dtolnay/rust-toolchain@1.88

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: msrv

      - run: cargo build -p strixonomy-lsp -p strixonomy-cli --bins

      - run: cargo test --workspace

  smoke:
    name: CLI smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - run: |
          set -euo pipefail
          cargo run -- query fixtures "SELECT * FROM classes"
          cargo run -- validate fixtures
          cargo run -- query fixtures "SELECT code FROM diagnostics"
          cargo run -- sparql fixtures "SELECT ?s WHERE { ?s ?p ?o } LIMIT 1"

  release-build:
    name: release build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release

      - run: cargo build --release --locked -p strixonomy-cli -p strixonomy-lsp --bins

      - run: ./target/release/strixonomy inspect fixtures

  lsp-build:
    name: LSP build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - run: cargo build --locked -p strixonomy-lsp --bins

      - run: cargo test -p strixonomy-workspace --test lsp_smoke

      - run: cargo test -p strixonomy-workspace --test lsp_reasoner

  webview-ui:
    name: webview ui tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: extension/webview-ui/package-lock.json

      - run: npm ci
        working-directory: extension/webview-ui

      - run: npm test
        working-directory: extension/webview-ui

  extension-build:
    name: extension build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Build strixonomy-lsp for extension e2e tests
        run: cargo build -p strixonomy-lsp --bins

      - uses: actions/setup-node@v4
        with:
          # Match extension-vscode-e2e (@vscode/test-electron@3.1+ engines.node >= 22)
          node-version: "22"
          cache: npm
          cache-dependency-path: extension/package-lock.json

      - run: npm ci
        working-directory: extension

      - run: npm run compile
        working-directory: extension

      - name: Extension unit and bundled-LSP e2e tests
        run: npm test
        working-directory: extension
        env:
          STRIXONOMY_LSP_BIN: ${{ github.workspace }}/target/debug/strixonomy-lsp

      - name: VSIX unpack spawn e2e (Marketplace chmod regression)
        run: |
          set -euo pipefail
          PLATFORM=linux-x64
          mkdir -p "extension/server/${PLATFORM}"
          cp target/debug/strixonomy-lsp "extension/server/${PLATFORM}/strixonomy-lsp"
          chmod +x "extension/server/${PLATFORM}/strixonomy-lsp"
          cd extension
          npx vsce package --no-dependencies -o /tmp/strixonomy-ci.vsix
          rm -rf /tmp/strixonomy-vsix-unpack
          unzip -q /tmp/strixonomy-ci.vsix -d /tmp/strixonomy-vsix-unpack
          export STRIXONOMY_E2E_EXTENSION_ROOT=/tmp/strixonomy-vsix-unpack/extension
          npm test
        env:
          STRIXONOMY_LSP_BIN: ${{ github.workspace }}/target/debug/strixonomy-lsp

  audit:
    name: cargo audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - run: cargo audit

  package:
    name: crate packaging
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - run: |
          set -euo pipefail
          # Only leaf crates (no unpublished path deps) can dry-run here.
          # Full graph is published in dependency order on release tags.
          cargo publish -p strixonomy-core --dry-run
          cargo publish -p strixonomy-robot --dry-run
          cargo build -p strixonomy-diagnostics -p strixonomy-owl -p strixonomy-cli -p strixonomy

  docs:
    name: mkdocs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install MkDocs
        run: pip install -r docs/requirements.txt

      - name: Build documentation
        # Fast path: git-revision stamps off (see mkdocs.yml / build-docs.sh).
        # RTD still publishes stamps via READTHEDOCS=True.
        run: ./scripts/build-docs.sh
